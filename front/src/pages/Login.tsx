import { LoginCard } from "@/components/auth/LoginCard"

export default function Login() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-main-background p-6">
      <LoginCard />
    </div>
  )
}