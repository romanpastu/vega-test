export const ANIMATION_DURATION = {
  DEFAULT: 500,
  FAST: 300,
  SLOW: 700,
} as const

// Tailwind classes for common animations
export const ANIMATION_CLASSES = {
  DEFAULT_TRANSITION: 'transition-all duration-500 ease-in-out transform',
} as const 